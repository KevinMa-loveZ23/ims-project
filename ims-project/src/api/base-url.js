export {
  baseUrl, websocketUrl
}

// "https://test.kein-thema.xyz/api"

// const basePath = "localhost:9998/api"

// const baseUrl = "http://" + basePath
// const websocketUrl = "ws://" + basePath + "/websocket"

const basePath = "test.kein-thema.xyz/api"

const baseUrl = "https://" + basePath
const websocketUrl = "wss://" + basePath + "/websocket"
